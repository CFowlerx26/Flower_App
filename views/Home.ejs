<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <% pageTitle %> </title>
</head>
<body>
    <h1>Indulge in these natural creations</h1>
    <ul>
        <% data.map( (flowers, home) => { %>
                <li>
                    <%= fruit.name %> is
                    <img src = <%=`${flowers.img}.jpg`%> width ="400" height ="300">
                </li>
        <% }) %>
   </ul>
    
</body>
</html>